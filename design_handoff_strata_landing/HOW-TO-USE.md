# Strata landing — handoff to Claude Code

Open this folder in your editor. Then in Claude Code, run:

```
Read design_handoff_strata_landing/README.md and implement the Strata
landing page as a Next.js 14 App Router project in TypeScript. Use the
HTML/CSS/JSX in reference/ as the visual spec — match pixel-for-pixel.
Drop the Tweaks panel; it's design-time only.
```

That's it. The README is self-sufficient.

Want screenshots of each section appended to the README? Ask the design agent
to add them — they were omitted by default to keep the bundle lean.
